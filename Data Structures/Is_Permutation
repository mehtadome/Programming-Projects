#1.2 Python | Given 2 strings, determine if one is a permutation of the other
# For the strings to be a permutation of one another, they must have the same characters and have the same length

class Solution:

  def Is_Permutation(string1, string2):
    if (len(string1) == len(string2)):
      for char1 in string1:  
        for char2 in string2:
          if ((char1 in string2) and (char2 in string1)):
            return True
          else:
            return False
  
# Longer time complexity is a minus to this solution
# Takes about O(n^3)

# Another way:

def is_perm(str1, str2):
  if(len(str1) == len(str2) and sorted(str1) == sorted(str2)):
    return True
  else:
    return False
   
