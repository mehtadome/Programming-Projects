#1.1 Python | Implement an algorithm that returns True if a string has all unique characters

class Solution:

  def Is_Unique(stringie):
    uniqChar = set()
    for i in stringie:
      if (i in uniqChar):
        return False
      uniqChar.add(i)
    return True

# A plus to this solution is a shorter time complexity
